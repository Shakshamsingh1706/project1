# Copy to .env and set values. Used by storefront_app via dotenv-rails.

RAILS_ENV=production
SECRET_KEY_BASE=generate_with_rails_secret

# MySQL (used by config/database.yml)
DB_NAME=spree_production
DB_USER=spree
DB_PASS=password
DB_HOST=127.0.0.1

# Optional: app host for URLs
# APP_HOST=your-server-ip
