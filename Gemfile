# Gemfile for deployment (run cap from repo root)
# Usage: bundle install && bundle exec cap production deploy

source "https://rubygems.org"

gem "capistrano", "~> 3.20"
gem "capistrano-bundler"
gem "capistrano-rails", "~> 1.6"
gem "capistrano-rbenv", "~> 2.2"
